version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build step. Serving static files."
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
